# Known Edge Cases — Momo (local only — never commit)

Copy to `known-edge-cases-momo.md` (gitignored) and fill in confirmed patterns for `momo-extraction.md`. Check this table before flagging a row as `NEEDS REVIEW`. Add a row here only once a pattern is confirmed — by the user directly, or by an identical case recurring and being confirmed — never speculatively.

| Pattern | Resolution |
|---|---|
